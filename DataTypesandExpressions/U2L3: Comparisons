arr = ["zebra", "kangaroo", "cat", "human", "aardvark"]

for i in range(len(arr)):
  for j in range(i, len(arr)):
    if arr[i] > arr[j]:
      temp = arr[j]
      arr[j] = arr[i]
      arr[i] = temp
      
print(arr)
